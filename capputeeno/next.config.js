/** @type {import('next').NextConfig} */
const nextConfig = {
// indicar para o compilador do next que está sendo utlizado styled comp., dispensa o uso de plugins
    compiler: {
        styledComponents: true
    }
}

module.exports = nextConfig
